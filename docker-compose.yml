version: '3.8'

services:
  gemini-proxy:
    build: .
    container_name: gemini-proxy-service
    ports:
      - "8000:8000"
    restart: unless-stopped
    # --- Optional Configuration ---
    # environment:
      # Uncomment and set the variables below to change the default behavior.
            # - UPSTREAM_GEMINI_ENDPOINT=https://generativelanguage.googleapis.com

      # Determines where to inject the text.
      # Valid values: "SUFFIX" (default), "PREFIX"
      # - INJECTION_POSITION=SUFFIX

      # Determines what content to inject.
      # Valid values: "TIMESTAMP" (default), "UUID"
      # - INJECTION_MODE=UUID